const Tower = (laneType, towerType, posX, posY, imgURL) => {
  return {
    laneType: laneType,
    towerType: towerType,
    posX: posX,
    posY: posY,
    imgURL: imgURL,
  };
};

export default Tower;
